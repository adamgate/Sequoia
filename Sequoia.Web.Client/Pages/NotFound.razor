@page "/not-found"

@inject NavigationManager navigationManager

<h3>Not Found</h3>
<p>Sorry, the content you are looking for does not exist.</p>
<button @onclick=RedirectToHome>Redirect</button>

@code {
    public void RedirectToHome()
        => navigationManager.NavigateTo("/");
}